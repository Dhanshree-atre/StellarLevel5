# Compact contract boundary

This directory is reserved for the deployed Midnight Compact contract and generated bindings. The frontend deliberately does not submit fake transactions. Add the existing Level 1-4 Compact source, generated API, deployment address, and SDK adapter here before enabling live Lace actions.

Required configuration is documented in `.env.example`.
